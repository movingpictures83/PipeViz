if [[ ! -e $1 ]]
then
  printf "File $1 not found.\n"
  exit 1
fi

dot_file=$1.dot
replace_parameter_files $1 > normalized_config.txt
parse_pipe.awk normalized_config.txt > $dot_file
rm normalized_config.txt

if [[ $2 == png ]]
then
  dot -Tpng -O $dot_file
fi
